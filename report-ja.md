# gpt-ossのPython実行とウェブ検索機能に関する調査報告書

gpt-ossがどのようにPythonスクリプトを実行し、ウェブ検索を実行するかというご質問にお答えするため、gpt-ossのコードベースを調査しました。以下に調査結果の概要を記載します。

## Pythonスクリプトの実行

`gpt-oss`は、**Docker**を使用して、安全で隔離された環境でPythonスクリプトを実行します。このプロセスは`gpt_oss/tools/python_docker/docker_tool.py`にある`PythonTool`によって管理されています。

主な手順は以下の通りです：
1.  `python:3.11`イメージを使用して、一時的な**Dockerコンテナ**が作成されます。
2.  Pythonスクリプトがコンテナにコピーされます。
3.  スクリプトがコンテナ内で実行され、その**標準出力（`stdout`）がキャプチャ**されます。
4.  実行後、コンテナは**直ちに破棄**され、各実行がステートレスで安全であることが保証されます。

## ウェブ検索機能

ウェブ検索は、従来の組み込みブラウザではなく、外部APIに依存するモジュール化された**ブラウザツール**を使用して実行されます。関連ファイルは`gpt_oss/tools/simple_browser/simple_browser_tool.py`と`gpt_oss/tools/simple_browser/backend.py`です。

プロセスは次のように機能します：
1.  `SimpleBrowserTool`は、エージェントにブラウジングコマンド（例：`search`、`open`）を提供します。
2.  このツールは、実際のウェブ要求をバックエンドに委任します。提供されている特定の実装は`ExaBackend`であり、これは**Exa Search API**を使用します。
3.  検索を実行するために、バックエンドはクエリをExa APIに送信し、結果をエージェント用にフォーマットします。
4.  ページを表示するために、バックエンドはExa APIを介してページのコンテンツを取得します。

## 結論

`gpt-oss`は、サンドボックス化されたPython実行にDockerを使用し、ウェブブラウジングにはExa Search APIを使用して、安全性と機能性の両方を確保しています。
